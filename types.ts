export interface AppSettings {
    feel: string;
    mood: string;
    speed: string;
    vocalStyle: string;
    instrumentation: string[];
    autoExpand: boolean;
    mastering: string;
    storyMode: boolean;
    storyPrompt: string;
}